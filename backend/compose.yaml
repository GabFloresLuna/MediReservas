name: medireservas-databases

services:
  mysql-auth:
    image: mysql:8.0
    container_name: mysql_auth_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: auth_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3311:3306"
    volumes:
      - auth_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  mysql-users:
    image: mysql:8.0
    container_name: mysql_users_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: users_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3312:3306"
    volumes:
      - users_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  mysql-doctors:
    image: mysql:8.0
    container_name: mysql_doctors_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: doctors_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3313:3306"
    volumes:
      - doctors_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  mysql-specialties:
    image: mysql:8.0
    container_name: mysql_specialties_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: specialties_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3314:3306"
    volumes:
      - specialties_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  mysql-schedules:
    image: mysql:8.0
    container_name: mysql_schedules_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: schedules_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3315:3306"
    volumes:
      - schedules_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  mysql-appointments:
    image: mysql:8.0
    container_name: mysql_appointments_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: appointments_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3316:3306"
    volumes:
      - appointments_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  mysql-payments:
    image: mysql:8.0
    container_name: mysql_payments_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: payments_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3317:3306"
    volumes:
      - payments_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  mysql-medical-records:
    image: mysql:8.0
    container_name: mysql_medical_records_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: medical_records_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3318:3306"
    volumes:
      - medical_records_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  mysql-prescriptions:
    image: mysql:8.0
    container_name: mysql_prescriptions_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: prescriptions_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3319:3306"
    volumes:
      - prescriptions_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  mysql-notifications:
    image: mysql:8.0
    container_name: mysql_notifications_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: notifications_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3320:3306"
    volumes:
      - notifications_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  mysql-reports:
    image: mysql:8.0
    container_name: mysql_reports_db
    restart: unless-stopped
    command:
      - --innodb-buffer-pool-size=128M
      - --max-connections=30
      - --performance-schema=OFF
    mem_limit: 384m
    mem_reservation: 192m
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: reports_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3321:3306"
    volumes:
      - reports_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

volumes:
  auth_data:
  users_data:
  doctors_data:
  specialties_data:
  schedules_data:
  appointments_data:
  payments_data:
  medical_records_data:
  prescriptions_data:
  notifications_data:
  reports_data:
